import 'package:flutter/material.dart';

class JSQuestionsScreen extends StatelessWidget {
  const JSQuestionsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("JavaScript Questions here"),
      ),
    );
  }
}
