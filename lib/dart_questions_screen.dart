import 'package:flutter/material.dart';

class DartQuestionsScreen extends StatelessWidget {
  const DartQuestionsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Dart Questions here"),
      ),
    );
  }
}
