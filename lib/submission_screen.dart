import 'package:flutter/material.dart';

class SubmissionScreen extends StatelessWidget {
  const SubmissionScreen({super.key});
  @override
  Widget build(Object context) {
    return const Scaffold(
      body: Center(
        child: Text("Submission Screen here"),
      ),
    );
  }
}
