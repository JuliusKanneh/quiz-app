import 'package:flutter/material.dart';

class PhPQuestionsScreen extends StatelessWidget {
  const PhPQuestionsScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("PHP Questions here"),
      ),
    );
  }
}
